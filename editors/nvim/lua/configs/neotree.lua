require("neo-tree").setup({
    filesystem = {
        filtered_items = {
            visible = true,
            hide_hidden = false
        },
        window = {
            mappings = {
                ["h"] = "open",
            }
        }
    }
})
