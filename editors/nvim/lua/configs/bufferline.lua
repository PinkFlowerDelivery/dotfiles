require("bufferline").setup({
    options = {
        buffer_close_icon = '',
        show_buffer_icons = false,
        show_buffer_close_icons = false,

        indicator = {
            icon = '',
            style = ''
        }
    }
})
